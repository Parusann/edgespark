# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# EdgeSpark artifacts, regenerable, and some are large
checkpoints/
data/cache/
data/datasets/pairs.jsonl
runs/**/*.jsonl
runs/plots/
!runs/reference/summary.json      # keep the committed modelled reference

# Models / weights
*.pt
*.safetensors
*.gguf

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local agent tooling (never commit)
.claude/
.mcp.json

# LaTeX build artifacts (keep the .tex, .bib, and the compiled .pdf)
*.aux
*.log
*.bbl
*.blg
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
